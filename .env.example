# Copy to .env and restart the dev server (Expo inlines EXPO_PUBLIC_* at build time).

# Backend base URL.
EXPO_PUBLIC_API_URL=https://endurancepace-api.coachonurai.com

# "true" = use the in-memory MockApi + sample data, no backend / no auth needed.
EXPO_PUBLIC_USE_MOCK_API=false
